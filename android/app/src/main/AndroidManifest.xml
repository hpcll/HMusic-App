<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- audio_service 后台/锁屏播放：前台媒体服务 + 熄屏保活 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- App 内自更新（设置 → 关于与更新）：下完 APK 交系统安装器。
         直装渠道（GitHub Release / 网盘）需要它；上架商店的版本必须去掉这行，
         商店渠道由商店自己更新（见 docs/11-release-compliance.md）。 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:label="HMusic"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- 自更新用：把 cacheDir/updates 里的 APK 以 content:// 授权给系统
             安装器（Android 7+ 不接受 file://）。见 MainActivity.install。 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.updates"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <!-- Android 端玻璃后端 = Impeller。2026-09-04 前曾全局关闭：Impeller(Vulkan)
             在部分 Adreno 驱动上进入/离开榜单详情会整屏花屏一下（iQOO Z10 Turbo Pro /
             骁龙 8s Gen 4 / Android 16，画面状态栏以下横向重复约 4 次，约 100ms 自愈，
             定位为后端本身问题）。小米 25019PNF3C（天玑 9400 / Mali）真机 spike：
             进出榜单详情 ×10 零花屏、logcat 零 GPU 报错，滚动帧 p95=2.38ms /
             max=3.73ms（120Hz 预算内 0 超帧）——Impeller 保持开启；附带收益是
             TopEdgeScrim 的渐进模糊 shader 在 Android 生效（此前固定纯纱帘）。
             约束：iQOO（Adreno）复测通过前，带此状态的构建不进 release 发布；
             若 Adreno 仍花屏，回退方案是 ImpellerBackend=opengles 或按 GPU 降级。 -->

        <!-- audio_service：媒体前台服务与耳机/蓝牙媒体按钮接收器 -->
        <service
            android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>
        <receiver
            android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>

# 更新日志

## Unreleased

## 0.1.6 - 2026-09-03

- **App 内直接更新**：设置 → 关于与更新，点「下载并安装」就地下载新版并交系统安装器，
  带真实百分比进度和取消，不再跳去 GitHub 页面。首次会请你打开「安装未知应用」授权。
  仅直装渠道（GitHub Release / 网盘）可用；iOS、桌面和商店版仍走各自的更新方式。
- **有新版会在设置入口点红点**：每次开 App 静默检查一次（6 小时节流，结果落盘，离线也
  记得上次的结果），提示只出现在 dock 的设置图标和「关于与更新」行上，不弹窗、不横幅。
- **安卓包按架构分开发布**：新增 `-arm64-v8a` / `-armeabi-v7a` / `-x86_64` 三个包，App 内
  更新只下本机那一份（约 25MB，通用包 61MB）。通用包 `-android.apk` 保留，手动下载仍可用。
- **修复检查更新一律报「网络不通或超时」**：挂着代理也这么说。真实原因多半是 GitHub 匿名
  接口限流（同一出口 IP 每小时 60 次，共享代理很容易占满，返回 403）。现在按限流 / HTTP
  状态码 / 连接超时 / 响应超时 / 证书 / DNS 分别报，超时从 8 秒放宽到 15 秒；GitHub 拉不到
  时还会退到仓库 `app-config.json`（Gitee + raw + jsDelivr 三镜像 + 服务端中转）。
- **榜单和搜索结果：点整行就播**，行上的播放钮去掉；行尾只留「下载入库」和「加入队列」。
  入库位三态同宽——没入库是 ↓、下载中转菊花、已入库是灰对勾，切换时右侧按钮不会横向漂移。
- **榜单和搜索的歌可以一键存到服务器曲库**：进页面时读一次下载记录，已入库的行直接标出来；
  下载完成停在原页面也会自己变成对勾（3 秒轮询，全部落地即停）。两页共用同一份索引，
  在一边下的歌到另一边也是已入库。
- 新增开关「播放过的在线歌自动入库」（设置 → 本地下载，默认关）：开了以后任何页面点播的
  在线歌都会顺手存一份到服务器，下次免解析、不怕直链过期；代价是服务器硬盘会慢慢变大。
- **修复安卓底部 dock 与手势条重叠**：底距原先两个平台共用 iOS 的算法（安全区 − 10），
  而安卓手势条的安全区几乎就等于那颗胶囊本身，减完正好压上去。安卓改为让开整条安全区
  再留 8，三键导航机型同样不压。
- **修复搜索结果页底部没有沉浸**：列表视口原先被 `SafeArea` 截在手势条上沿，结果滚不到
  胶囊后面。现在铺到屏幕底缘，让位由列表自身的内边距承担（与队列页同一套纪律）。
- **修复连接页键盘让位**：输入框聚焦时内容与键盘同步上来、连接按钮完整露出、收起回位。
  根因在引擎：安卓 15+ 强制 edge-to-edge，但键盘动画每帧上报的 inset 少减了一个导航条
  高度，动画结束才换成完整值——读起来就是「内容跟上来一点、停稳后又自己跳一下」。
  现在显式声明 edge-to-edge，动画帧与最终帧口径一致；同时删掉了此前五版手写的让位机制。
- 连接页键盘动画和滚动不再整屏重绘：内容整列自成一层、几何不再随键盘每帧重排。

## 0.1.5 - 2026-08-30

- 修复**只有先暂停上一首才能播下一首**：列表里点一首歌开播后，所有播放键一直变灰按不动。
  根因是本机开播那一步 `await` 了播放器的 `play()`——`just_audio` 的这个 Future 要等到
  「播完 / 被暂停 / 被停」才落地，于是整条播放命令挂满一首歌的时长，前台的播放互斥锁一直不放。
  现在开播只发指令、不等它结束。
- 修复**明明暂停了，却在那一刻弹出「正在播放 XXX」**：同一个根因的另一副面孔——成功提示排在
  播放命令之后，命令挂到暂停才返回，提示就跟着延到暂停那一刻才出现。顺带修掉的还有自动连播
  偶发失效（重入守卫整首歌不复位）。
- 修复**播放队列页底部没有沉浸**：列表视口原先被 `SafeArea` 截在手势条上沿，最后一行下面留出
  一条不透明底板。现在内容铺到屏幕最底，让位改由列表自身的内边距承担，滚到底时最后一行仍完整
  露在手势条上方。
- macOS 改发 **dmg**（`hmusic-<版本>-macos-universal.dmg`，取代原来的 `-macos-universal-adhoc.zip`）：
  挂载后把 HMusic 拖进旁边的「应用程序」即可安装。仍是 ad-hoc 签名、未公证，首次打开依旧要走
  「系统设置 → 隐私与安全性 → 仍要打开」。
- 校验值不再是每个包各带一个 `.sha256`，统一汇总进一个 `hmusic-<版本>-SHA256SUMS.txt`，
  Release 资产从 14 条降到 8 条。核验：`shasum -a 256 --ignore-missing -c hmusic-<版本>-SHA256SUMS.txt`。
- 配套提示：**第三方音源在 App 里点了就播放失败、网页端却正常**，修复在服务端 0.2.3（音频代理
  不再把播放器的 `User-Agent` 透传给上游 CDN），请把服务端一并升级。

## 0.1.4 - 2026-08-29

- 修复 Android 正式包**一个插件都没注册**的发布脚本缺陷。`tool/build_release.sh` 先删掉生成的
  `GeneratedPluginRegistrant.java`，再用 `--no-pub` 构建；而这个文件只在 pub 步骤重新生成，
  `--no-pub` 恰好把这一步跳过，于是打出来的包里没有任何插件注册代码。FlutterEngine 找不到注册表
  只打一行 warning 就继续跑，所以包能装、能开、能连服务端、能拉列表，但插件通道全是空的：
  - 点任何一首歌都报 `MissingPluginException(... audio_service.client.methods)`——本机曲库和
    音源直链一样播不了，同一台服务端的网页端却正常（网页端不经过 App 插件）；
  - 安全存储写不进去，token 只活在内存里，于是「每次打开都要重新登录」；
  - 本地键值存储同样失效——0.1.2 记的 `SharedPreferencesAsync channel-error` 是同一个根因，
    之前两次「失败就降级」的兜底只把它藏得更深。
  现在构建不再跳过 pub（release 模式的 pub 自己会剔除 integration_test 这类 dev 依赖插件，
  手删文件本就没必要），并在打包后机械校验注册表内容与 APK 里确有注册代码，缺一样就构建失败。
- 冷启动接续上次连接的服务器：开屏直接连回去并进入登录页，token 有效就继续放行到首页，不再每次
  都要从「附近的服务器」重新点一台——点到的地址形态（mDNS 给 `host.local`、扫段给 IP）与上次存的
  不一致时会被当成换了服务器而清掉 token，这是「每次打开要重新登录」的另一条路径。接续失败
  （换了网络、服务端没开机）静默回落到自动扫描，不打扰用户。
- 修复部分机型进出榜单详情时**整屏花屏一下**：画面在状态栏以下被横向重复约 4 次，约 100ms 后自愈，
  进入和返回两个方向都会出现，但不是必现，也只在部分设备上出现。用三个只差一处的诊断包实测定位到
  Impeller(Vulkan) 后端本身——只关掉顶缘的 shader 渐进模糊仍然花，关掉 Impeller 就干净
  （iQOO Z10 Turbo Pro / 骁龙 8s Gen 4 / Android 16，来回切 10 次零复现）。Android 因此改回 Skia
  渲染后端。代价是顶缘渐进模糊在 Android 上退成同色纱帘（依旧无缝，只是没有磨砂）；根因大概率在
  引擎/驱动侧，升级 Flutter 引擎后会重新实测，能不花就换回 Impeller。

## 0.1.3 - 2026-08-28

- 修复登录页卡在「处理中…」：钥匙串/Keystore 通道失效时 `SecureTokenStore` 的异常会整个逃出
  提交流程，把状态永久留在 submitting。现在写不进持久层只降级为本次会话驻留内存，登录与连接
  流程还额外加了兜底捕获——任何意外都会退出忙碌态并报出原因，不再冻住按钮。
- README 与安装文档补齐 macOS/Windows 未签名包的放行步骤：说明“已损坏，应移到废纸篓”只是 Gatekeeper
  的措辞，并纠正 macOS 15 起“右键打开”已失效、须走系统设置的变化。

## 0.1.2 - 2026-08-28

- 修复部分 Android 机型「能发现服务端但连接报错」：本地键值存储在 `SharedPreferencesAsync`
  的 DataStore 通道失效（`PlatformException: channel-error`）时自动降级到 legacy
  SharedPreferences，两级都不可用则退到进程内内存。保存服务器地址、升级缓存和本机音量
  不再因为本地存储故障中断连接流程。
- Android 构建号（versionCode）开始随发版递增：`pubspec.yaml` 的版本改为 `0.1.2+2`，
  此前所有包的 versionCode 都固定是 1。

## 0.1.1 - 2026-08-28

- 准备公开发布的安装、贡献、安全和构建文档。
- 增加 Android APK/AAB 的可复现构建脚本与 SHA-256 校验文件。
- 增加 Windows x64 中文安装向导，支持开始菜单、可选桌面快捷方式和卸载。
- Linux 便携包加入应用图标和桌面入口资源。

版本发布时将记录兼容性变化、迁移步骤和已知限制。版本号遵循 `pubspec.yaml` 与 Git tag 对齐的规则。
